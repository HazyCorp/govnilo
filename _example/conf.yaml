serve:
  port: 15559

controller:
  sync_interval: 1s

metrics:
  port: 14448

settings_provider:
  # from_admin:
  #   async:
  #     refresh_interval: 1s
  #     timeout: 1s
  #   transport:
  #     grpc:
  #       target: 'adminka:8500'
  from_file:
    path: ./settings.yaml

logging:
  mode: json
  level: debug
  filter:
    infra:
      enabled: true
      level: info

redis:
  host: localhost
  port: 6379
  username: govnilo
  password: "govnilo-rulit"
  db: 0
