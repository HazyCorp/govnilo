services:
  unknown-service:
    target: 'abacaba:8080'
    checkers:
      vvv:
        success_points: 2
        fail_penalty: 1
        rate:
          times: 1
          per: 1s
      zzz:
        success_points: 2
        fail_penalty: 1
        rate:
          times: 1
          per: 1s

  example:
    target: 'example.service:8000'
    checkers:
      sleeper:
        check:
          success_points: 1
          fail_penalty: 1
          rate:
            times: 1
            per: 1s
        get:
          # get check is empty, we don't need to run it
          success_points: 0 
          fail_penalty: 0
          rate:
            times: 0
            per: 1s
