services:
  example:
    target: 'example.service:8000'
    sploits:
      example_attack:
        run_options:
          rate:
            times: 1
            per: 1s
          max_goroutines: 0
    checkers:
      sleeper:
        check:
          success_points: 1
          fail_penalty: 1
          run_options:
            rate:
              times: 0
              per: 1s
            max_goroutines: 0
        get:
          # get check is empty, we don't need to run it
          success_points: 0 
          fail_penalty: 0
          run_options:
            rate:
              times: 0
              per: 1s
            max_goroutines: 0
